{#- Arrivals (first night) by lodging group, for drivers, passengers and the undecided. -#}
{%- set groups = [
  ['v1', ['Village 1']],
  ['v2', ['Village 2']],
  ['v4', ['Apartments']],
  ['v5', ['Village 5', 'Village 6']],
  ['v7', ['Village 7']],
  ['vW', ['Wellness']],
  ['vT', ['RV Camping', 'Tent Camping']],
] -%}
{%- set kinds = [['D', 'Driver'], ['P', 'Passenger'], ['N', 'Not sure yet']] -%}
{%- set totals = {} -%}
{%- for camper in campers if camper.lodging and camper.stay -%}
    {%- set arrival = (camper.stay | sort)[0] -%}
    {%- for suffix, driving in kinds if camper.attributes.driving == driving -%}
        {%- for key, places in groups -%}
            {%- for place in places if place in camper.lodging.path_names -%}
                {%- do totals.update({arrival ~ key ~ suffix: totals.get(arrival ~ key ~ suffix, 0) + 1}) -%}
            {%- endfor -%}
        {%- endfor -%}
    {%- endfor -%}
{%- endfor -%}
{%- for suffix, driving in kinds -%}
## Arrivals: {{ driving }}

| Day | Village1 | Village2 | Village4 (apt) | Village5&6 | Village7 | Wellness | RV/Tent | Total |
| --- | -------- | -------- | -------- | ---------- | -------- | -------- | ------- | ----- |
{% for night in event.nights -%}
{%- set ns = namespace(total=0) -%}
| {{ night | date('%a') }} |{% for key, places in groups %} {{ totals.get(night ~ key ~ suffix, 0) }} |{% set ns.total = ns.total + totals.get(night ~ key ~ suffix, 0) %}{% endfor %} {{ ns.total }} |
{% endfor %}
{% endfor -%}
