#!/bin/bash

docker compose exec -T django python manage.py migrate
docker compose exec -T django python manage.py flush --noinput
docker compose exec -T django python manage.py createsuperuser --noinput

# Import every event, then run the post-import snapshot tests against it.
docker compose run --rm data /bin/sh -c "import && npm run test:live"
