# This file is here so you can override the default settings as needed

from camphoric_server.settings_default import *  # noqa: F403 F401

CSRF_TRUSTED_ORIGINS = [
    'https://{{ camphoric_domain_name }}',
    'http://{{ lookup('dig', ansible_host) }}:8000',
    'http://{{ lookup('dig', ansible_host) }}:3000',
    'http://localhost:8000',
    'http://localhost:3000',
    'http://localhost:3001',
    'http://localhost:3002',
    'http://localhost:3003',
]
ALLOWED_HOSTS = [
    '{{ camphoric_domain_name }}',
    '{{ lookup('dig', ansible_host) }}',
    'localhost',
]

# Backups: configured via STORAGES['dbbackup'] in settings_default.py, driven by the
# DBBACKUP_STORAGE_OPTIONS environment variable (written to server/.env by django.env.j2).
# django-dbbackup rejects the legacy DBBACKUP_STORAGE* settings, so do not set them here.


