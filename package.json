{
  "scripts": {
    "loadData": "docker-compose exec react node fixtures/lark/loadData.mjs",
    "django-test": "docker-compose exec django python manage.py test",
    "heroku-postbuild": "cd client && yarn install && yarn run build"
  },
  "devDependencies": {
    "chalk": "^4.1.2",
    "inquirer": "^8.2.0",
    "node-fetch": "^3.1.0"
  },
  "dependencies": {}
}
